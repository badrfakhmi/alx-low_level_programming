#include<stdio.h>

  /**
  *main - print the alphabitic
  *Return: Always (Success)
  */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
